export const peopleAtLeastYearsOld = (people, age) => undefined;

export const cityNames = people => undefined;

export const personByName = (people, name) => undefined;

export const averageAge = people => undefined;

// Note: Object.groupBy() is not yet supported in all major browsers
export const groupPeopleByCity = people => undefined;

export const indexOfTheOldestPerson = people => undefined;

export const commaSeparatedNames = people => undefined;

// Note: There is no native partition method in JavaScript
export const shortAndLongNamed = (people, nameLengthLimit) => undefined;

export const languagesInCity = (people, city) => undefined;
